#include <stdio.h>

// INT is defined in config.h which is included by the forced include
INT main () {
  printf ("ForcedInclude: check if --include works\n");
  printf ("=============================================================\n");
  printf ("fine if it compiles\n");
  printf ("=============================================================\n");
}
